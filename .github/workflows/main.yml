on:
  schedule:
  - cron:  "*/15 * * * *"
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Read Node.js version to install from `.tool-versions`
        id: fetch-data
        #run: echo '::set-output name=test::green'
        uses: tongchen92/hello-world-javascript-action@v1.3
      - name: Overwrite file
        uses: "DamianReeves/write-file-action@master"
        with:
          path: path/to/file.js
          write-mode: overwrite
          contents: |
            "${{ steps.fetch-data.outputs.time }}"
            
      - name: Commit & Push
        uses: Andro999b/push@v1.3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          force: true
          message: 'Overwritten by Github Actions - ${date}'
# jobs:
#   hello_world_job:
#     runs-on: ubuntu-latest
#     name: A job to say hello
#     steps:
#       - name: Hello world action step
#         id: hello
#         uses: tongchen92/hello-world-javascript-action@v1.1
#         with:
#           who-to-greet: 'Tong Chen'
#       # Use the output from the `hello` step
#       - name: Get the output time
#         run: echo "The time was ${{ steps.hello.outputs.time }}"
#       - name: switching from HTTPS to SSH
#         run: git remote set-url origin git@github.com:tongchen92/hello-world-javascript-action.git
#       - name: stage changed files
#         run: git add .
#       - name: commit changed files
#         run: git commit -m "Auto updating student.json"
#       - name: fetch from main
#         run: git fetch origin main
#       - name: push code to main
#         run: git push origin HEAD:main